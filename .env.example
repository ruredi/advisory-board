# API keys — ne commitold GitHub-ra!
# Másold a secret-project/.env-ből, vagy hagyd üresen: automatikus fallback ../secret-project/.env-re.

# Scrapfly — social media scrape (X, Instagram, Facebook)
SCRAPFLY_KEY=

# Supadata — social videó transcript (YouTube, X, Instagram, TikTok, Facebook URL)
SUPADATA_API_KEY=

# OpenAI — embeddings + social carousel image OCR (gpt-4o)
OPENAI_API_KEY=

# Opcionális
# GOOGLE_API_KEY=
# QDRANT_URL=http://localhost:6333
# SUPADATA_USD_PER_CREDIT=0.015
